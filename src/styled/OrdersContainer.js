import styled from "styled-components";

export const OrdersContainer = styled.div`
  max-width: 420px;
  div {
    display: flex;
    flex-flow: row nowrap;
    button {
      font-size: 0.7rem;
    }
  }
`
